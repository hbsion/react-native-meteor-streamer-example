import { Meteor } from 'meteor/meteor';

Meteor.startup(() => {
  // code to run on server at startup
  // for (var i = 0; i < 20; i++) {
  //   Todos.insert({
  //     todo: i.toString(),
  //     ts: new Date()
  //   });
  // }
});
